# Convex Configuration
CONVEX_SELF_HOSTED_URL=
CONVEX_SELF_HOSTED_ADMIN_KEY="self-hosted-convex|..."

# Better Auth client (exposed to Vite/Nuxt)
VITE_CONVEX_SITE_URL=

# Encryption key for provider API keys (64-char hex string = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Claude Code OAuth (server-level authentication)
# Preferred: Use the dashboard UI to authenticate via web OAuth flow.
# Tokens are automatically persisted to .data/claude-code-tokens.json.
#
# Alternative: Set these env vars to pre-configure tokens (e.g. in headless environments).
# Obtain by running `claude login` on the server and extracting tokens.
# CLAUDE_CODE_ACCESS_TOKEN=
# CLAUDE_CODE_REFRESH_TOKEN=
# CLAUDE_CODE_EXPIRES_AT=
